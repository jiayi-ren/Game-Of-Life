import React, { useState } from 'react';

const Game = props => {

    const [speed, setSpeed] = useState(100); // speed, 1 generation/100ms
    const [rows, setRows] = useState(50); // row
    const [cols, setCols] = useState(50);
    return (
        <div>

        </div>
    )
};

export default Game;